# Render-compatible Apache listen configuration.
# The container must listen on $PORT (Render injects it at runtime).
# ListenAll directive is set in the entrypoint via sed on this file.
Listen ${PORT}
